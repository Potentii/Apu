export default class ImportSelection{

	/**
	 *
	 * @param {*} data
	 * @param {Boolean} is_selected
	 */
	constructor(data, is_selected=false){
		this.data = data;
		this.is_selected = is_selected;
	}

}